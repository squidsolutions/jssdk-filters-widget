<div class="squid-api-date-selection-widget">
    {{#if dateAvailable}}
		<span>{{dateDisplay}}</span>
    {{else}}
        {{#if facet}}
                <span>select a date range</span>
            {{else}}
                no date available
        {{/if}}
    {{/if}}
    {{#if computing}}
        click to refresh
    {{/if}}
</div>
